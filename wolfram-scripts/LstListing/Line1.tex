In[#]:= MyNewFunc[x_] := x/0; MyNewerFunc[x_] := x[[3]]; $ThisDirectory = If[NotebookDirectory[] == $Failed, Directory[], NotebookDirectory[], NotebookDirectory[]]; $MagicString = "is longer than depth of object"; FunctionCheck::culpret = "Function `1` will now act on the expression `2`."; (*omitted 1055 characters for brevity*) ]; MultipleSteps[InputExpr_] := Module[{Expr = InputExpr, NewStream = OpenWrite[], FileContentData}, Block[{$Messages = Append[$Messages, NewStream]}, Expr //= FunctionCheck[MyNewerFunc]; Expr = 1; Expr //= FunctionCheck[MyNewFunc]; ]; Discriminator[NewStream[[1]]]; Close[NewStream]; Expr];